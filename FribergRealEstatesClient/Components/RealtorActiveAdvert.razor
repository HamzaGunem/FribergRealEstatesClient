@* Created by Robert*@

<!-- Layout för större skärmar -->
<div class="only-above-1440">
    <MudCard Class="responsive-card" Style="height: 330px; display: flex; flex-direction: row; overflow: hidden; border: 1px solid gray;">
        <MudCardMedia Image="@ImageUrl" Style="width: 400px; height: 100%; object-fit: cover;" />

        <div style="flex: 1; display: flex; flex-direction: column; padding: 16px; height: 100%;">
            <div class="mb-1">
                <MudText Typo="Typo.h6">@Title</MudText>
            </div>
            <div class="mb-2">
                
                <MudText Typo="Typo.body2" Style="color: gray"><MudIcon Icon="fas fa-home" Color="Color.Primary" />
                    &nbsp @City, @Commune kommun
                </MudText>
            </div>
            <div class="row">
                <div style="display: flex; flex-direction: row; justify-content: space-between; height: 100%; width: 80%">
                    <MudText Typo="Typo.body1"><b>@CurrentPrice kr</b></MudText>
                    <MudText Typo="Typo.body1">
                        <b>
                            @Area m²@if (BiArea != null && BiArea > 0)
                            {
                                   <span>+ @BiArea m²</span>
                            }
                        </b>
                    </MudText>
                    <MudText Typo="Typo.body1"><b>@Room rum</b></MudText>
                    <MudText Typo="Typo.body1">
                        <b>
                            @if (Floor != null && Floor > 1)
                            {
                                <span>vån @FloorRow</span>
                            }
                        </b>
                    </MudText>

                </div>
            </div>
            <div>
                <div class="mb-2" style="display: flex; flex-direction: row; justify-content: space-between; height: 100%; width: 40%">
                    <MudText Typo="Typo.body2" Style="color: gray">@MonthlyFee kr/mån</MudText>
                    <MudText Typo="Typo.body2" Style="color: gray">@OperatingCost kr/m²</MudText>
                </div>
            </div>

            <div class="mb-2" style="flex: 1; display: flex; align-items: center;">
                <MudText Typo="Typo.subtitle2" Class="description-truncate">
                    @Description
                </MudText>
            </div>
            <div class="m-0">
                @if (Facilities != null && Facilities.Length > 0)
                {
                    foreach (var item in Facilities)
                    {
                        <MudChip T="string" Variant="Variant.Outlined">@item</MudChip>
                    }
                }                
            </div>
            <hr />
            <div>
                <MudText Typo="Typo.h6" Style="color: gray">@AgencyName</MudText>
            </div>
        </div>
    </MudCard>
</div>

<!-- Layout för små skärmar -->
<div class="only-below-1440">
    <MudCard Class="responsive-card" Style="overflow: hidden; border: 1px solid gray;">
        <MudCardMedia Image="@ImageUrl"
                      Style="object-fit: cover; Height:300px;" />

        <MudCardContent>
            <div>
                <MudText Typo="Typo.h6">@Title</MudText>
            </div>
            <div class="mb-2 mt-2">

                <MudText Typo="Typo.body2" Style="color: gray">
                    <MudIcon Icon="fas fa-home" Color="Color.Primary" />
                    &nbsp @City, @Commune kommun
                </MudText>
            </div>
            <div class="row">
                <div style="display: flex; flex-direction: row; justify-content: space-between; height: 100%; width: 80%">
                    <MudText Typo="Typo.body1"><b>@CurrentPrice kr</b></MudText>
                    <MudText Typo="Typo.body1">
                        <b>
                            @Area m²@if (BiArea != null && BiArea > 0)
                            {
                                <span>+ @BiArea m²</span>
                            }
                        </b>
                    </MudText>
                    <MudText Typo="Typo.body1"><b>@Room rum</b></MudText>
                    <MudText Typo="Typo.body1">
                        <b>
                            @if (Floor != null && Floor > 1)
                            {
                                <span>vån @FloorRow</span>
                            }
                        </b>
                    </MudText>

                </div>
            </div>
            <div>
                <div class="mb-2" style="display: flex; flex-direction: row; justify-content: space-between; height: 100%; width: 40%">
                    <MudText Typo="Typo.body2" Style="color: gray">@MonthlyFee kr/mån</MudText>
                    <MudText Typo="Typo.body2" Style="color: gray">@OperatingCost kr/m²</MudText>
                </div>
            </div>

            <div class="mb-1" style="flex: 1; display: flex; align-items: center;">
                <MudText Typo="Typo.subtitle2" Class="description-truncate">
                    @Description
                </MudText>
            </div>
            <div>
                @if (Facilities != null && Facilities.Length > 0)
                {
                    foreach (var item in Facilities)
                    {
                        <MudChip T="string" Variant="Variant.Outlined">@item</MudChip>
                    }
                }
            </div>
            <hr />
            <div class="p-0">
                <MudText Typo="Typo.h6" Style="color: gray">@AgencyName</MudText>
            </div>
        </MudCardContent>
    </MudCard>
</div>

<style>
    .description-truncate {
        overflow: hidden;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
        text-overflow: ellipsis;
    }
</style>

@code {
    [Parameter] public string ImageUrl { get; set; }
    [Parameter] public string Title { get; set; }
    [Parameter] public string CurrentPrice { get; set; }
    [Parameter] public int Area { get; set; }
    [Parameter] public int? BiArea { get; set; }
    [Parameter] public int Room { get; set; }
    [Parameter] public int? Floor { get; set; }
    [Parameter] public int FloorRow { get; set; }
    [Parameter] public string MonthlyFee { get; set; }
    [Parameter] public string OperatingCost { get; set; }
    [Parameter] public string[] Facilities { get; set; }
    [Parameter] public string City { get; set; }
    [Parameter] public string Commune { get; set; }

    [Parameter] public string Description { get; set; }

    [Parameter] public string AgencyName { get; set; }
}
